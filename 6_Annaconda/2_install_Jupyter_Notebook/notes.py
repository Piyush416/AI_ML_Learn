# create a folder in this call installation
# conda activate -> base env activated 
# conda install juypter
# jupyter notebook

# jupyter notebook file extension is .ipynb means -> i python notebook(ipynb)

# in this notebook we have shell so we can run single chunks of code no entire code