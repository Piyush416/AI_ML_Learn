CREATE DATABASE IF NOT EXISTS college;

use college;

CREATE TABLE teacher(
id INT PRIMARY KEY,
name VARCHAR(30) NOT NULL,
subject VARCHAR(30),
salary INT
);

INSERT INTO teacher 
(id, name, subject, salary)
VALUES
(23, "ajay", "math", 50000),
(47, "bharat","english", 60000),
(18, "chetan", "chemistry", 45000),
(9, "divya", "physics", 75000);

-- select the teacher having salary more than 55k
select name from teacher where salary > 55000; 

-- Rename the salary column of teacher table to ctc
ALTER TABLE teacher CHANGE COLUMN SALARY CTC INT;

SET SQL_SAFE_UPDATES = 0;
-- UPDATE SALARY TO ALL TEACHERS BY GIVING THEM AN INCREMENT OF 25%
UPDATE TEACHER SET CTC = CTC + CTC*(0.25);

-- ADD A NEW COLUMN CITY AND SET DEFAULT VALUE "GURGAON"
ALTER TABLE teacher 
ADD COLUMN city VARCHAR(30) DEFAULT "Gurgaon"; 
select * from teacher;

-- Delete the Salary column from teacher table
ALTER TABLE teacher DROP COLUMN ctc; 
select * from teacher;